# 一种面向岸基扫描雷达的图形处理器海杂波逐脉冲实时生成方法

## 摘要

岸基机械扫描雷达在对海监视过程中需要随天线转动逐脉冲输出距离向复回波。与常见的离线 CPI 杂波序列生成不同，逐 PRT 回波模拟不仅要求海杂波满足幅度分布、多普勒功率谱和时间相关性等统计特征，还要求生成过程能够跟随机械扫描波束实时变化，并能够在长时间仿真中保持可复现、可调参和可接入后端处理链路的工程能力。传统预生成、公共长序列随机起点读取以及全图状态递推方法虽然能够在一定程度上降低实时计算压力，但在存储量、参数在线调整、散射单元独立性和任意 PRT 复现方面存在不足。

本文结合当前回波模拟项目中的 GPU 海杂波生成模块，研究一种面向岸基扫描雷达的逐脉冲实时生成方法。该方法将海面离散为距离-方位 patch，在每个 PRT 根据当前波束指向筛选有效照射 patch；利用 Counter-based 随机数生成器按索引直接产生任意 patch、任意历史脉冲处的白噪声样本；通过 FIR 滤波实现单 patch 慢时间多普勒谱成形；再结合瑞利、Weibull、对数正态和 K 分布等幅度模型完成 patch 级随机散射建模。GPU 端采用距离维并行方式生成当前 PRT 的距离向复回波，避免保存完整距离-方位-时间杂波数据和大规模 speckle 历史状态。实验部分将从单 patch 幅度分布、单 patch 功率谱、逐 PRT 生成速度和一圈扫描 PPI 导出演示四个方面进行验证。

关键词：岸基扫描雷达；海杂波；GPU；逐 PRT 生成；Counter-based RNG；FIR 谱成形

## 1. 绪论

### 1.1 研究背景

对海监视雷达回波模拟器的作用，是在实验室条件下为后端信号处理、目标检测、恒虚警处理、显示系统和硬件接口联调提供可控输入。真实海试受海况、目标配合、场地条件和试验成本限制，难以反复构造完全相同的测试场景。因此，软件回波模拟器需要能够生成可重复、可调参、可解释的目标回波、海杂波和热噪声。

海杂波是对海监视雷达中最重要的环境干扰之一。它不仅影响距离向和方位向背景强度，也会影响动目标显示、杂波抑制、CFAR 门限估计以及弱小目标检测结果。对于回波模拟系统而言，海杂波模块不能只给出“有背景噪声”的图像效果，而应能够说明其幅度分布、多普勒谱和扫描过程是如何形成的。

岸基机械扫描雷达具有明显的逐脉冲工作特征。天线波束随时间连续转动，每发射一个脉冲，当前距离向回波主要来自当前方位附近主瓣及有效旁瓣覆盖的海面散射单元。随着 PRT 推进，多个距离向回波共同形成一圈距离-方位图像。同一片海面可能在相邻 PRT 内连续受照，也可能在下一扫描圈再次进入波束。因此，岸基扫描雷达海杂波生成需要同时考虑统计正确性、扫描几何关系和实时输出能力。

### 1.2 问题描述

当前项目中的海杂波模块需要按 PRT 输出距离向复回波。以 PRF 为 1600 Hz 为例：

```text
PRT = 1 / 1600 = 625 us
```

这意味着每个 PRT 的杂波生成时间应尽量控制在数百微秒量级。若采用完整距离-方位-时间数据预生成，存储压力和参数调整成本较高；若采用公共长序列随机起点读取，不同距离单元之间可能引入非物理相关性；若对全场所有 patch 每个 PRT 都递推状态，又会造成大量计算浪费，因为当前波束实际只照射一小部分方位区域。

因此，本文关注的问题是：如何在不预先保存完整杂波序列、不为每个 patch 保存长历史状态的前提下，利用 GPU 在每个 PRT 实时生成当前波束下的海杂波距离向复回波，并保持单 patch 幅度分布、多普勒谱和随机结果可复现。

### 1.3 本文工作内容

本文围绕当前项目中的 GPU 海杂波生成部分展开，主要内容包括：

1. 梳理传统海杂波生成和 GPU 加速杂波仿真的研究现状，分析其与岸基扫描雷达逐 PRT 生成需求之间的差距。
2. 建立距离-方位 patch 级海杂波组织方式，说明当前 PRT active patch 的筛选和相干叠加流程。
3. 给出基于 Counter-based RNG 的随机样本索引方法，用于按需生成 FIR 所需历史白噪声。
4. 说明单 patch 多普勒谱成形和幅度分布建模方法。
5. 给出 GPU 端逐 PRT 生成流程和实验验证框架。

## 2. 研究现状与问题分析

### 2.1 雷达模拟器中的实时性处理方法

雷达回波模拟器首先要解决的是实时输出问题。罗晶心《雷达模拟器实时性研究与设计实现》主要从雷达模拟器工程实现角度讨论实时性设计。该文献中提到的典型处理思路包括两类：一类是将计算复杂、实时生成代价较高的数据提前计算并存储，在系统运行时直接调用；另一类是利用已经产生的数据替代当前时刻需要生成的随机数据，从而减少实时计算量。该方法路线的核心是“以存储换计算”或“以数据复用换实时性”，适合计算资源有限而输出节拍要求较严格的模拟器系统。

这种思路对本文有直接启发：逐 PRT 海杂波生成同样受到实时节拍约束，不能在每个脉冲内做无限复杂的全场计算。但该类方法也存在明显不足。预先存储会带来数据量和真实度之间的矛盾，数据复用又可能破坏随机性。对于岸基扫描雷达海杂波，如果采用完整预生成，一旦海况、谱宽、幅度分布或扫描参数改变，就需要重新生成大量数据；如果复用旧随机序列，则难以保证每个距离-方位散射 patch 的独立性和长时间仿真的可解释性。

### 2.2 基于预生成序列和随机起点的杂波生成方法

查连忠《基于 DSP 的雷达地物杂波模拟与实现》采用的是较典型的预生成序列使用方式。该方法先在计算机上对某一个距离单元生成杂波序列，并将数据放入 DSP 主板的程序存储器 Boot Flash 中。在 DSP 初始化阶段，通过随机抽取序列不同片段的方式，将其作为不同距离单元上的杂波序列。该方案适合 DSP 平台实时计算能力有限的情况，优点是运行时只需要读取和输出，实时压力较小。

滕琨《基于 GPU+CPU 的雷达仿真系统设计与实现》也采用了类似的杂波调制序列预生成思路。其做法是将每个 CPI 所需的杂波随机调制序列事先产生并保存到文件中，程序初始化时读入内存；每次产生杂波时使用随机函数得到一个地址偏移，再从该偏移位置开始读取一帧杂波随机序列。该方法能够降低运行阶段的随机序列生成开销，并通过随机起点在一定程度上避免每次完全相同。

上述两类方法本质上都是“长序列 + 随机起点”或“预生成 + 回放”。它们适合传统模拟器中对实时性要求较高、对空间散射独立性要求不强的场景。但对于本文关注的岸基机械扫描雷达，问题在于：不同距离单元或方位单元可能来自同一条长序列的不同片段，空间上不一定对应独立海面散射单元；当仿真时间较长时，序列片段可能重复或相关性增强；当需要改变海况、功率谱或幅度分布参数时，预生成序列需要重新生成。本文希望避免把杂波看作少数几条长序列的回放，而是把每个距离-方位 patch 看作独立随机散射单元，并在当前 PRT 按需生成。

### 2.3 基于 ZMNL/SIRP 的 GPU 杂波序列加速方法

徐国伟《基于 GPU 并行计算的雷达杂波模拟研究》重点研究了 GPU 在杂波随机序列生成中的加速作用。该文献针对 ZMNL 方法生成 Weibull 分布杂波时卷积计算耗时的问题，将卷积计算转化为适合 GPU 执行的批量矩阵运算，并利用 CUDA、cuBLAS 等工具提高大规模序列生成效率。对于相关 K 分布杂波，文献采用 SIRP 思路，即将 K 分布杂波表示为慢变 texture 与快变 speckle 的乘积，并使用 GPU 加速高斯变量生成、卷积和非线性变换过程。

该研究说明了两个重要结论。第一，Weibull、K 分布等非高斯杂波可以通过“相关高斯序列 + 非线性变换”或“SIRP texture-speckle”结构生成。第二，杂波序列生成中的卷积、随机数生成和非线性变换具有较强数据并行性，适合放到 GPU 上加速。这些结论为本文采用 FIR 谱成形、ZMNL 幅度变换和 GPU 并行计算提供了方法支撑。

但是，该类研究的主要对象是大规模杂波序列本身，通常先生成一段满足统计特性的长序列，再供后续仿真使用。其重点是提高“生成一批杂波序列”的速度，而不是研究机械扫描雷达在每个 PRT 中如何根据当前波束只生成 active patch 的回波。因此，该类方法没有直接解决本文中的三个问题：第一，如何在波束旋转过程中按 PRT 输出距离向复回波；第二，如何避免为全场所有 patch 维护 FIR 历史状态；第三，如何在不从头递推的情况下复现任意 patch 在任意 PRT 的随机样本。

### 2.4 面向散射单元的 GPU 海杂波回波仿真方法

王振兴《GPU 加速的机载雷达 K 分布海杂波仿真》面向机载雷达场景，采用距离环和方位角划分海面杂波单元，在 GPU 上并行计算杂波参数和距离环杂波累积，从而提高 K 分布海杂波仿真速度。该方法的特点是没有只停留在单条时间序列层面，而是将海面划分为空间散射单元，并在 GPU 上进行单元级计算和累积。这与本文采用距离-方位 patch 建模具有相似之处。

不过，机载雷达与岸基机械扫描雷达的几何关系和数据组织不同。机载平台存在平台运动、距离环、多普勒展宽和观测几何快速变化等问题，其仿真重点往往是机载观测条件下的距离环杂波累积。本文关注的是固定岸基平台上天线机械扫描带来的逐 PRT 方位照射变化。也就是说，本文更关心“当前波束照到哪些方位 patch”“这些 patch 如何在相邻 PRT 中连续进入和离开波束”“如何按 PRT 输出距离向回波”，而不是一次性生成某个机载观测场景下的批量距离环杂波。

### 2.5 CPU+GPU 多通道雷达海杂波建模方法

雷赫《基于 CPU+GPU 架构的机载多通道雷达海杂波建模仿真方法研究》建立了较完整的机载多通道雷达海杂波回波模型。该方法使用 AR 模型生成具有脉冲相关性的杂波序列，并在 GPU 上并行计算每个散射单元的方位角、俯仰角、散射系数、天线增益、空时相移因子、线性调频信号时延、杂波增益和回波求和。该研究说明，对于包含大量散射单元、通道和脉冲的雷达回波仿真，CPU 负责调度和参数组织，GPU 负责大规模散射单元并行计算，是一种有效结构。

该文献与本文最接近之处在于：它不是简单生成一条杂波随机序列，而是把散射单元的几何参数、天线增益、信号时延和回波叠加纳入 GPU 计算流程。本文同样采用“散射 patch 建模 + GPU 并行求和”的思想。但该文献主要面向机载多通道相控阵雷达，输出组织通常是距离-脉冲-通道批量数据，相关性构建采用 AR 模型。对于岸基机械扫描雷达逐 PRT 生成而言，仍有几个问题需要进一步处理：AR 或高阶递推模型需要保存每个散射单元状态；如果 patch 在当前 PRT 不处于波束内，是否仍要更新其状态会影响计算量；若需要直接复现某个 PRT 的结果，递推状态方法通常不如按索引直接生成方便。本文采用 Counter-based RNG 和 FIR 按需重构历史白噪声，正是为了减少这类状态维护和随机访问问题。

### 2.6 基于非线性变换的杂波单元回波方法

中国专利申请 CN120779340A《基于非线性变换法的雷达模拟器地杂波回波的实现方法》公开了一种基于 ZMNL 的地杂波回波生成方法。该方法将雷达探测范围划分为多个杂波单元，结合地形高度数据计算杂波单元散射面积，使用相干视频回波模型和 ZMNL 法计算每个杂波单元的回波，最后对各杂波单元回波进行矢量叠加得到总回波。

该方法体现了杂波回波模拟中的几个基本环节：空间单元划分、单元散射面积或散射强度计算、非线性变换生成指定幅度分布、相干视频回波建模以及多个杂波单元矢量叠加。本文同样采用 patch 级建模和相干叠加思想，也考虑 ZMNL 在 Weibull、对数正态等分布中的作用。不同之处在于，该专利重点是基于非线性变换法实现地杂波回波，并未针对岸基机械扫描雷达的逐 PRT 流式输出，也没有讨论 GPU 上如何按需生成任意历史脉冲的白噪声样本。

### 2.7 小结

现有研究为本文提供了多个方面的支撑：预生成和缓存回放方法说明了雷达模拟器实时性压力的来源；长序列随机起点方法说明了工程上常用的低计算量近似；ZMNL 和 SIRP 方法提供了 Weibull、K 分布等非高斯杂波的建模依据；机载 GPU 海杂波研究说明了距离-方位散射单元划分和 GPU 并行累积的可行性；CPU+GPU 多通道仿真研究说明了复杂回波模型可以采用 CPU 调度、GPU 并行计算的架构。

但这些方法仍不能完全满足本文的具体需求。本文面向的是岸基机械扫描雷达，要求每个 PRT 根据当前波束指向实时输出距离向复回波；当前 PRT 只需要计算 active 方位 patch，而不希望对全场 patch 做无效更新；同时还要求单 patch 的幅度分布和多普勒谱可验证，随机结果可由 seed 和索引复现。因此，本文在已有研究基础上，将距离-方位 patch 建模、Counter-based RNG、FIR 谱成形和 GPU 距离维并行结合起来，形成适合当前项目的逐 PRT 海杂波实时生成流程。

## 3. GPU 海杂波逐脉冲生成方法

### 3.1 距离-方位 patch 建模

本文将雷达观测海面离散为距离和方位两个维度：

```text
range index   r = 0, 1, ..., N_r - 1
azimuth index a = 0, 1, ..., N_az - 1
```

每个 `(r, a)` 表示一个等效海面散射 patch。该 patch 的平均回波强度由后向散射系数、patch 面积、雷达方程、距离衰减、海况参数和天线方向图等因素决定。对第 `n` 个 PRT，机械扫描模型给出当前波束中心方位 `theta_beam(n)`，再根据天线方向图和门限确定当前 PRT 的 active 方位 patch 集合：

```text
Omega(n) = { a | G(a, theta_beam(n)) >= threshold }
```

这样，每个 PRT 不需要遍历完整 360 度方位网格，只需要计算当前波束附近贡献显著的 patch。对某一距离单元 `r`，当前 PRT 的海杂波可表示为：

```text
z(r, n) = sum_{a in Omega(n)} W(r, a, n) * c(r, a, n)
```

其中，`c(r, a, n)` 是 patch 级随机散射复包络，`W(r, a, n)` 包含距离权重、平均杂波幅度和方向图电压权重。该组织方式将“散射单元统计建模”和“当前波束空间加权叠加”分开，便于解释和验证。

### 3.2 Counter-based RNG 与谱成形

传统随机数生成器通常依赖递推状态。若要得到第 `n` 个样本，需要从前一状态继续推进，或者保存该随机流的当前状态。对于每个距离-方位 patch 都维护随机状态和 FIR delay line，会造成较大的存储压力。

Counter-based RNG 将随机数看作由索引决定的函数：

```text
random_sample = F(seed, range_idx, az_idx, source_id, pulse_idx)
```

只要 seed 和索引一致，任意时刻、任意线程调用都能得到相同随机样本。本文将距离索引、方位索引、随机源编号和全局 PRT 序号编码到 counter 中。不同随机过程使用不同 `source_id`，例如 speckle 白噪声、K 分布 texture、热噪声和目标起伏等。

为了控制单 patch 的慢时间多普勒谱，先由 Counter-based RNG 生成复白高斯序列 `w(r, a, n)`，再经过 FIR 滤波：

```text
y(r, a, n) = sum_{k=0}^{L-1} h[k] * w(r, a, n-k)
```

其中，`h[k]` 为根据目标多普勒谱设计并归一化的 FIR 系数。传统 FIR 需要保存每个 patch 的历史白噪声；本文方法中，历史项 `w(r, a, n-k)` 由 counter 直接重算，因此不保存 speckle delay line。其代价是每个 PRT 内增加 RNG 计算量，但该计算结构规则、并行度高，适合 GPU 执行。

### 3.3 幅度分布建模

FIR 输出保持复高斯形式时，复包络幅度服从瑞利分布，功率服从指数分布。为了支持更一般的海杂波幅度统计，可在 patch 级引入非线性变换或 SIRP 模型。

Weibull 分布和对数正态分布可通过 ZMNL 方法实现。基本思路是保留相关复高斯序列的相位，对幅度进行非线性映射，使输出幅度满足目标分布。K 分布可采用 SIRP 表达：

```text
K-distributed clutter = sqrt(texture) * speckle
```

其中 speckle 表示快变复高斯散射，texture 表示慢变强度调制。texture 可以由独立随机源生成并通过 Gamma 映射得到。若要求 texture 具有慢时间连续性，需要额外维护慢变 texture 状态或低频过程；但该状态量远小于为所有 patch 保存 FIR 历史白噪声。

### 3.4 GPU 端逐 PRT 执行流程

当前项目中的 GPU 基线流程采用距离维并行，即一个 GPU thread 负责一个距离单元。每个 PRT 的执行过程如下：

```text
输入：当前 PRT 序号 n、当前波束指向、杂波参数

CPU:
  1. 计算当前波束方位和俯仰。
  2. 根据方向图门限生成 active 方位 patch 集合。

GPU:
  1. 每个 thread 处理一个距离单元 r。
  2. 对当前 active patch 集合逐个遍历。
  3. 对每个 patch 使用 Counter RNG 生成 FIR 所需历史白噪声。
  4. 执行 FIR 谱成形和幅度分布变换。
  5. 乘以距离权重和天线方向图权重。
  6. 对 active patch 贡献进行相干叠加。

输出：当前 PRT 的距离向复回波 z_clutter[0 ... N_r-1]
```

该流程的主要计算量近似为：

```text
N_range x N_active_azimuth x L
```

其中 `N_active_azimuth` 为当前波束下参与叠加的方位 patch 数，`L` 为 FIR 长度。方位 patch 越密、方向图门限越低、FIR 越长，单 PRT 计算量越高。本文正文只讨论当前一维距离并行基线，二维并行和多流流水优化作为后续工作展开。

## 4. 实验设计与验证方案

本章用于搭建后续实验报告框架。具体硬件配置、参数和测试结果需要在实际运行后补充。

### 4.1 实验环境与仿真参数

实验环境建议按表 1 填写。

| 项目 | 配置 |
|---|---|
| CPU | 待填写 |
| GPU | 待填写 |
| 内存 | 待填写 |
| 操作系统 | 待填写 |
| CUDA 版本 | 待填写 |
| 编译器 | 待填写 |
| CMake 构建类型 | 待填写 |
| 项目提交或分支 | 待填写 |

主要仿真参数建议按表 2 填写。

| 参数 | 数值 |
|---|---:|
| PRF | 待填写 |
| PRT | 待填写 |
| 距离单元数 | 待填写 |
| 方位 patch 间隔 | 待填写 |
| 方位 patch 数 | 待填写 |
| active gain floor | 待填写 |
| FIR 长度 | 待填写 |
| 多普勒谱宽 | 待填写 |
| 扫描角速度 | 待填写 |
| 一圈 PRT 数 | 待填写 |
| 随机种子 | 待填写 |
| 幅度分布类型 | 待填写 |

### 4.2 单 patch 统计特性验证

该实验用于验证单个距离-方位 patch 的随机散射过程是否满足设定统计特性。测试时固定 `range_idx`、`az_idx`、`seed` 和分布参数，连续生成一段慢时间 IQ 序列。首先统计幅度直方图、经验 CDF、均值和方差，并与瑞利、Weibull、对数正态或 K 分布理论曲线进行对比。若采用瑞利分布，还应检查功率是否近似指数分布。

需要放入报告的图包括：

- 单 patch I/Q 慢时间序列图。
- 单 patch 幅度时间序列图。
- 幅度 PDF 直方图与理论曲线对比图。
- 幅度 CDF 与理论 CDF 对比图。
- 瑞利分布情况下的功率指数分布对比图。

项目中对应的测试入口为：

```text
test/export_clutter_patch_trace.cpp
test/validate_clutter_patch_trace.m
```

本节结论可围绕“幅度统计是否符合设定分布”“均值方差是否合理”“重尾分布是否体现出更强的大幅度样本”展开。

### 4.3 单 patch 功率谱验证

该实验用于验证 FIR 谱成形是否有效。测试时固定一个 patch，导出足够长的复 IQ 慢时间序列，采用 Welch 方法估计归一化功率谱，并与目标高斯谱或设定谱形进行对比。

需要放入报告的图包括：

- 单 patch 归一化 Doppler PSD 与目标谱形对比图。
- 主谱区局部放大图。

本节需要补充的数值包括 FIR 长度、目标谱宽、Welch 参数、谱峰位置误差和主瓣宽度误差。该实验是证明“Counter RNG 只是白噪声寻址方式，真正的时间相关性由 FIR 谱成形保证”的关键支撑。

### 4.4 逐 PRT 速度测试

该实验用于验证 GPU 生成模块是否满足实时输出要求。测试时设置距离单元数、方位 patch 间隔、FIR 长度、active gain floor 和扫描角速度，连续生成一圈或多圈扫描数据，记录总 PRT 数、总运行时间、平均单 PRT 耗时、每秒生成 PRT 数和实时倍率。

结果建议按表 3 填写。

| 指标 | 结果 |
|---|---:|
| 总 PRT 数 | 待填写 |
| 总运行时间 | 待填写 |
| 平均单 PRT 耗时 | 待填写 |
| PRT/s | 待填写 |
| 实时倍率 | 待填写 |
| active patch 最小值 | 待填写 |
| active patch 最大值 | 待填写 |
| active patch 平均值 | 待填写 |

建议补充的图包括：

- 不同距离单元数下的平均单 PRT 耗时。
- 不同 FIR 长度下的平均单 PRT 耗时。
- 不同 active gain floor 或 active patch 数下的平均单 PRT 耗时。

项目中对应的测试入口为：

```text
test/benchmark_clutter_gpu_rotation.cpp
```

本节结论应明确比较平均单 PRT 耗时与 PRT 周期。例如 PRF 为 1600 Hz 时，实时门限为 625 us。若平均单 PRT 耗时低于该值，可说明当前参数下具备实时生成能力；若略高于该值，则需要结合 active patch 数、FIR 长度和 GPU 型号分析瓶颈。

### 4.5 一圈扫描 PPI 导出演示

该实验主要用于展示逐 PRT 生成结果如何随机械扫描形成一圈距离-方位图像。测试时连续生成一圈扫描数据，保存距离-方位功率矩阵和指定距离单元的 IQ 序列，再绘制距离-方位功率图、PPI 图、距离向平均功率曲线和指定距离单元随方位变化曲线。

需要放入报告的图包括：

- 一圈扫描距离-方位功率图。
- 一圈扫描 PPI 图。
- 距离向平均功率曲线。
- 指定距离单元随方位变化的功率曲线。

项目中对应的测试入口为：

```text
test/export_clutter_rotation_ppi.cpp
test/show_clutter_rotation_ppi.m
```

需要说明的是，PPI 图主要作为流程演示，用于证明模块能够逐 PRT 输出并形成完整扫描图像。严格的模型正确性仍应以单 patch 幅度分布和功率谱验证为主要依据。

## 5. 总结与展望

本文针对岸基机械扫描雷达逐 PRT 回波模拟需求，整理了一种基于 GPU 的海杂波实时生成方法。该方法以距离-方位 patch 为基本散射单元，根据当前波束指向筛选 active patch；利用 Counter-based RNG 直接生成任意 patch、任意历史 PRT 的白噪声样本；通过 FIR 滤波形成目标多普勒谱；再结合 ZMNL 或 SIRP 方法实现不同幅度分布。与完整预生成、公共长序列随机起点读取和全图状态递推方法相比，该方法在存储占用、局部复现、GPU 并行适配和逐 PRT 实时输出方面更适合当前项目需求。

后续工作主要包括以下几个方向。第一，扩展二维静态散射强度图，将当前按距离幅度组织的杂波强度扩展为 `A_static[N_range x N_azimuth]`，以支持不同方位、不同区域、不同海况和海陆边界下的空间非均匀杂波。第二，在当前距离维并行基线基础上探索二维并行与归约，将线程映射扩展到 `(range, active_patch)`，以提高 active patch 较多或 FIR 较长时的并行度。第三，优化 GPU 端数据访问，例如将 FIR 系数放入 constant memory，将 active patch list 和方向图权重显式化，减少 kernel 内重复计算。第四，在系统级联调阶段研究多流和异步流水线，使海杂波生成、目标/噪声叠加、后续处理和数据输出能够更好地衔接。上述二维建模和流优化属于后续完善方向，本文主体仍以当前 GPU 逐 PRT 生成基线为重点。
