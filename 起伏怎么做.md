是的，如果你的杂波和回波系统改成了**逐 PRT/逐脉冲连续生成**，那么目标起伏模型也应该改成**逐 PRT 状态递推/状态更新**，而不是按固定 CPI 一块一块更新。

但是这里要注意一个关键点：

> **逐 PRT 生成，不等于所有目标起伏都必须每个 PRT 独立刷新。**  
> 起伏更新频率应该由目标起伏模型决定，而不是由处理 CPI 决定。

也就是说，你的目标回波生成要按 PRT 输出，但目标 RCS/散射幅度是否更新，要看你选的是 Swerling 0、I、II、III、IV，还是相关起伏模型。

---

# 1. 逐 PRT 回波生成时，目标模型总体形式

对于第 \(k\) 个目标，第 \(n\) 个 PRT 的目标回波可以写成：

\[
x_k(r,n)=A_k(n)\cdot h\left(r-R_k(n)\right)\cdot e^{j\phi_k(n)}
\]

其中：

- \(A_k(n)\)：第 \(n\) 个 PRT 的复幅度，包含 RCS 起伏、传播损耗、天线增益等
- \(h(\cdot)\)：距离单元响应或脉压响应
- \(R_k(n)\)：第 \(n\) 个 PRT 的目标距离
- \(\phi_k(n)\)：目标相位
- \(n\)：全局脉冲编号

目标复幅度一般可以拆成：

\[
A_k(n)=
\sqrt{
\frac{P_t G_t(n)G_r(n)\lambda^2\sigma_k(n)}
{(4\pi)^3 R_k^4(n)L}
}
\cdot e^{j\varphi_{\sigma,k}(n)}
\]

其中：

- \(\sigma_k(n)\)：目标瞬时 RCS
- \(\varphi_{\sigma,k}(n)\)：目标散射相位
- \(G_t(n),G_r(n)\)：当前 PRT 天线方向上目标处的收发增益

所以在逐 PRT 模式下，目标起伏主要体现在：

\[
\sigma_k(n)
\]

以及可能的散射相位：

\[
\varphi_{\sigma,k}(n)
\]

如何随 PRT 更新。

---

# 2. 最重要原则：起伏更新不要绑定处理 CPI

你老师要求“一个 PRT 一个 PRT 生成”，就是为了让后端可以自由选择：

- 8 点 MTD
- 16 点 MTD
- 半 CPI
- 滑窗 MTD
- 2 脉冲 MTI
- 3 脉冲 MTI

所以目标起伏也不能写成：

```matlab
每个 CPI 更新一次 RCS
```

因为 CPI 是后端处理窗口，不是目标物理属性。

正确方式是：

> 目标起伏由目标自己的状态机决定，回波生成器每个 PRT 查询当前目标 RCS 状态。

---

# 3. 常见 Swerling 模型在逐 PRT 下怎么更新

Swerling 模型本质上有两个维度：

1. **RCS 分布**
2. **RCS 更新周期**

---

## 3.1 Swerling 0 / 5：非起伏目标

目标 RCS 恒定：

\[
\sigma(n)=\bar{\sigma}
\]

逐 PRT 下每个脉冲都用同一个 RCS。

适合：
- 理想点目标
- 标校球
- RCS 稳定目标
- 早期算法调试

---

## 3.2 Swerling I：扫描到扫描起伏，单次照射内恒定

Swerling I 的典型定义是：

- RCS 服从指数分布
- 在一次扫描/一次照射期间保持不变
- 下一次扫描再重新抽取

即：

\[
\sigma \sim \text{Exponential}(\bar{\sigma})
\]

但是注意：  
现在你不是按 CPI 生成，而是按 PRT 生成，所以“保持不变”的区间不能再叫 CPI，而应该叫：

> **一次波束照射期 / dwell / beam crossing / scan visit**

对于机扫雷达，一个目标被主瓣扫过的时间大约是：

\[
T_{dwell}=\frac{\theta_{BW}}{\omega_s}
\]

例如：

- 转速 6 rpm
- 方位波束宽度 1.8°
- 角速度 36°/s

则：

\[
T_{dwell}=1.8/36=0.05s
\]

PRF = 1000 Hz，则一次照射大约：

\[
N_{dwell}=50
\]

个 PRT。

所以 Swerling I 在逐 PRT 下应该这样做：

- 目标第一次进入波束时，抽取一个 RCS
- 在这次波束扫过期间，所有 PRT 使用同一个 RCS
- 目标离开波束后状态保持或无输出
- 下一圈再次进入波束时，重新抽取 RCS

也就是说：

\[
\sigma_k(n)=\sigma_{k,\text{visit}}
\]

在同一次 visit 内保持不变。

---

## 3.3 Swerling II：脉冲到脉冲起伏

Swerling II 是：

- RCS 服从指数分布
- 每个脉冲独立变化

即：

\[
\sigma(n)\sim \text{Exponential}(\bar{\sigma})
\]

逐 PRT 生成时非常直接：

> 每个 PRT 都重新抽取一次目标 RCS。

即：

```matlab
sigma = exprnd(sigmaMean);
```

适合：
- 快速起伏目标
- 多散射中心相位快速变化
- 模拟脉间不相关起伏

---

## 3.4 Swerling III：扫描到扫描起伏，4 自由度

Swerling III：

- RCS 服从 4 自由度卡方分布
- 在一次照射内保持不变
- 下一次照射重新抽取

工程上可用 Gamma 分布表示：

\[
\sigma \sim \text{Gamma}(k=2,\theta=\bar{\sigma}/2)
\]

其中均值为：

\[
E[\sigma]=k\theta=2\cdot \frac{\bar{\sigma}}{2}=\bar{\sigma}
\]

逐 PRT 下更新方式和 Swerling I 类似：

- 每次目标进入波束时抽一次
- 本次 dwell 内保持不变
- 下一圈再更新

---

## 3.5 Swerling IV：脉冲到脉冲起伏，4 自由度

Swerling IV：

- RCS 服从 4 自由度卡方/Gamma 分布
- 每个 PRT 独立刷新

即：

\[
\sigma(n)\sim \text{Gamma}(k=2,\theta=\bar{\sigma}/2)
\]

逐 PRT 下：

```matlab
sigma = gamrnd(2, sigmaMean/2);
```

每个 PRT 更新一次。

---

# 4. 用表总结最清楚

| 模型         | RCS 分布          | 逐 PRT 下更新规则                     |
| ------------ | ----------------- | ------------------------------------- |
| Swerling 0/5 | 常数              | 永不更新                              |
| Swerling I   | 指数分布          | 每次扫描照射/每次目标进入波束更新一次 |
| Swerling II  | 指数分布          | 每个 PRT 更新一次                     |
| Swerling III | Gamma，形状参数 2 | 每次扫描照射/每次目标进入波束更新一次 |
| Swerling IV  | Gamma，形状参数 2 | 每个 PRT 更新一次                     |

最关键的是：

> Swerling I/III 不是按处理 CPI 更新，而是按“目标被波束照射的 visit”更新。  
> Swerling II/IV 才是按 PRT 更新。

---

# 5. 机扫雷达里，“一次照射 visit”怎么判定？

这是工程实现重点。

假设天线当前方位为：

\[
\theta_b(n)
\]

目标方位为：

\[
\theta_t(n)
\]

则角误差：

\[
\Delta \theta(n)=\text{wrapTo180}(\theta_t(n)-\theta_b(n))
\]

当：

\[
|\Delta \theta(n)| < K\cdot \theta_{BW}
\]

认为目标被波束照射。

其中：
- \(\theta_{BW}\)：3 dB 波束宽度
- \(K\)：工程门限，一般取 0.5、1、1.5 或根据方向图门限确定

更严谨地，用天线方向图判断：

\[
G(\Delta\theta)>G_{\text{threshold}}
\]

例如：
- 高于主瓣峰值 -20 dB，认为进入有效照射区
- 或高于 -30 dB，认为进入可见区

---

## 5.1 Swerling I/III 的状态机

目标状态可以设计成：

```matlab
target.isIlluminatedLast
target.rcsCurrent
target.visitID
```

每个 PRT：

1. 计算当前目标是否在波束内
2. 如果上一 PRT 不在波束内、当前 PRT 在波束内：
   - 认为进入一次新的 visit
   - 对 Swerling I/III 重新抽取 RCS
3. 如果仍在波束内：
   - 保持当前 RCS
4. 如果离开波束：
   - 不更新，等待下一次进入

伪代码：

```matlab
isIlluminated = beamGain > gainThreshold;

if isIlluminated && ~target.wasIlluminated
    % 新的一次波束照射
    target.rcsCurrent = drawRCS(target.model, target.rcsMean);
end

target.wasIlluminated = isIlluminated;
```

其中 `drawRCS` 对 Swerling I/III 有效。

---

# 6. 逐 PRT 下目标相位也要连续更新

目标起伏只是一部分。  
如果你逐 PRT 生成回波，目标相位必须连续，否则 MTD/Doppler 会错。

目标相位可以由距离决定：

\[
\phi(n)=-\frac{4\pi R(n)}{\lambda}
\]

或者递推：

\[
\phi(n)=\phi(n-1)+2\pi f_d(n)T_r
\]

其中：

\[
f_d(n)=\frac{2v_r(n)}{\lambda}
\]

注意符号根据你的基带约定定。

工程上更建议用距离相位：

\[
s_t(n)=A(n)e^{-j\frac{4\pi R(n)}{\lambda}}
\]

这样目标距离运动、多普勒天然一致。

---

# 7. 起伏 RCS 和目标相位的关系

需要区分两个相位：

## 7.1 运动相位
由目标距离决定：

\[
\phi_R(n)=-\frac{4\pi R(n)}{\lambda}
\]

它必须连续，决定多普勒。

## 7.2 散射相位
由目标散射中心变化引起：

\[
\varphi_\sigma(n)
\]

对于简单 Swerling 模型，可以这样处理：

- Swerling I/III：一次照射内散射相位固定，下一次照射随机
- Swerling II/IV：每个 PRT 散射相位随机

所以总相位：

\[
\phi(n)=\phi_R(n)+\varphi_\sigma(n)
\]

但是工程上要小心：

如果 Swerling II/IV 每个脉冲散射相位完全随机，那么目标在多普勒域会被严重展宽，甚至不像一个稳定 Doppler 点。  
这符合“脉间快速起伏目标”的极端假设，但不一定适合所有场景。

---

# 8. 更工程化的模型：相关 Swerling / 相关 RCS 起伏

如果你不希望 RCS 在每个 PRT 完全独立跳变，可以使用**相关起伏模型**。

这比经典 Swerling 更适合实物仿真。

---

## 8.1 复散射系数 AR 模型

定义目标复散射系数：

\[
\alpha(n)
\]

令：

\[
\alpha(n)=\rho\alpha(n-1)+\sqrt{1-\rho^2}\sqrt{\bar{\sigma}}w(n)
\]

其中：

\[
w(n)\sim \mathcal{CN}(0,1)
\]

则：

\[
\sigma(n)=|\alpha(n)|^2
\]

如果 \(\alpha\) 是复高斯，则 \(\sigma\) 服从指数分布，类似 Swerling I/II 的连续相关版本。

相关系数：

\[
\rho=e^{-T_r/T_c}
\]

- \(T_r\)：PRT
- \(T_c\)：目标起伏相关时间

如果：
- \(T_c \to \infty\)，近似非起伏或慢起伏
- \(T_c\) 很小，近似脉冲独立起伏

---

## 8.2 这种模型的优点

它非常适合你现在的逐 PRT 框架：

- 每个 PRT 都递推一次
- 后端可以任意取窗
- 起伏不会在 CPI 边界断裂
- 可通过 \(T_c\) 控制起伏快慢

这比“每 CPI 抽一次 RCS”工程上更自然。

---

# 9. 推荐你的工程实现方式

如果你是做工程实物，我建议不要只实现传统 Swerling，而是实现两套：

---

## 9.1 经典 Swerling 模型
用于标准算法测试：

- Swerling 0
- Swerling I
- Swerling II
- Swerling III
- Swerling IV

其中：
- I/III 按 visit 更新
- II/IV 按 PRT 更新

---

## 9.2 相关起伏模型
用于更真实连续仿真：

\[
\alpha(n)=\rho\alpha(n-1)+\sqrt{1-\rho^2}\sqrt{\bar{\sigma}}w(n)
\]

每个 PRT 状态递推。

这个模型可以叫：

- correlated Swerling
- AR fluctuation
- correlated RCS scintillation

---

# 10. MATLAB 伪代码示例

下面给一个逐 PRT 目标 RCS 更新函数框架。

```matlab
function target = updateTargetRCS_PRT(target, beamGain, gainThreshold, PRI)

isIlluminated = beamGain > gainThreshold;

switch target.swerlingType

    case 0
        % 非起伏
        target.rcsCurrent = target.rcsMean;

    case 1
        % Swerling I: 每次进入波束更新一次，指数分布
        if isIlluminated && ~target.wasIlluminated
            target.rcsCurrent = exprnd(target.rcsMean);
            target.scatterPhase = 2*pi*rand;
        end

    case 2
        % Swerling II: 每个 PRT 更新一次，指数分布
        if isIlluminated
            target.rcsCurrent = exprnd(target.rcsMean);
            target.scatterPhase = 2*pi*rand;
        end

    case 3
        % Swerling III: 每次进入波束更新一次，Gamma分布
        if isIlluminated && ~target.wasIlluminated
            target.rcsCurrent = gamrnd(2, target.rcsMean/2);
            target.scatterPhase = 2*pi*rand;
        end

    case 4
        % Swerling IV: 每个 PRT 更新一次，Gamma分布
        if isIlluminated
            target.rcsCurrent = gamrnd(2, target.rcsMean/2);
            target.scatterPhase = 2*pi*rand;
        end

    case 10
        % 相关起伏模型，复散射系数AR
        Tc = target.fluctCorrTime;
        rho = exp(-PRI/Tc);

        w = (randn + 1j*randn)/sqrt(2);

        target.alpha = rho * target.alpha + ...
            sqrt(1-rho^2) * sqrt(target.rcsMean) * w;

        target.rcsCurrent = abs(target.alpha)^2;
        target.scatterPhase = angle(target.alpha);

    otherwise
        error('Unknown Swerling type');
end

target.wasIlluminated = isIlluminated;

end
```

---

# 11. 逐 PRT 目标回波生成伪代码

```matlab
function [echo, target] = genTargetEchoOnePRT(target, radar, pulseIndex)

c = 3e8;
lambda = radar.lambda;
PRI = 1 / radar.PRF;

% 当前时间
t = (pulseIndex-1) * PRI;

% 更新目标运动状态
[targetRange, targetAz, targetVr] = getTargetKinematics(target, t);

% 当前天线方位
beamAz = radar.az0 + radar.scanRateDeg * t;
dAz = wrapTo180(targetAz - beamAz);

% 天线增益
beamGain = antennaGain(dAz, radar);

% 更新RCS起伏
target = updateTargetRCS_PRT(target, beamGain, radar.gainThreshold, PRI);

% 平均接收功率
R = targetRange;
Pt = radar.Pt;
G = beamGain;
sigma = target.rcsCurrent;
L = radar.loss;

Pr = Pt * G^2 * lambda^2 * sigma / ((4*pi)^3 * R^4 * L);

% 目标运动相位
phiRange = -4*pi*R/lambda;

% 散射相位
phiScat = target.scatterPhase;

% 复幅度
amp = sqrt(Pr) * exp(1j*(phiRange + phiScat));

% 映射到距离单元
echo = complex(zeros(radar.Nr,1));
rbin = round(R / radar.dR) + 1;

if rbin >= 1 && rbin <= radar.Nr
    echo(rbin) = echo(rbin) + amp;
end

end
```

如果你做脉压后距离像，可以这样映射。  
如果做脉压前 I/Q，则应将目标延迟信号叠加到 ADC 采样上。

---

# 12. 半 CPI / 任意 MTD 下会发生什么？

假设目标为 Swerling I：

- 一次主瓣照射内 50 个 PRT
- 你后端取 16 个 PRT 做 MTD
- 或取 25 个 PRT 做半 CPI
- 或滑窗取 32 个 PRT

只要这些 PRT 在同一次照射内，RCS 就是恒定的。  
所以 Doppler 积累稳定。

假设目标为 Swerling II：

- 每个 PRT RCS 都变
- 你取任意长度 MTD，目标幅度都会脉间起伏
- MTD 峰值会降低，谱会展宽，检测性能下降

这正是 Swerling II 的物理意义。

---

# 13. 一个非常关键的工程建议

对于实物仿真，我建议默认不要直接用 Swerling II/IV 的“脉冲完全独立”作为主模型。

因为它太剧烈，容易导致：
- 目标多普勒不稳定
- 相干积累收益异常降低
- 工程人员误判算法问题

更推荐：

```text
默认：Swerling 0 或 Swerling I
增强：相关起伏模型
专项测试：Swerling II/IV
```

尤其机扫搜索雷达中，很多目标在一个短 dwell 内可以近似认为 RCS 不变或缓慢变化。

---

# 14. 最终结论

在逐 PRT 生成框架下，目标起伏应该这样处理：

1. **目标回波每个 PRT 都生成**
2. **目标运动相位每个 PRT 连续更新**
3. **RCS 是否每个 PRT 更新，由起伏模型决定**
4. **Swerling I/III 按一次波束照射 visit 更新，不按 CPI 更新**
5. **Swerling II/IV 按每个 PRT 更新**
6. **更工程化的做法是使用相关 RCS 起伏模型，每个 PRT 递推状态**
7. **后端 CPI/半 CPI/MTD 点数只是处理窗口，不应该反向影响目标起伏更新**

一句话总结：

> 逐 PRT 生成时，目标起伏要做成“目标自身状态机”。每个 PRT 输出回波，但 RCS 的刷新节奏由 Swerling 类型或相关时间决定，而不是由处理 CPI 决定。